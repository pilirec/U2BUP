# 真实频道盘点对开发优先级的影响

2026-09-21（用户本地日期），通过已授权的本机服务只读刷新频道元信息。完整研究报告、标题样本和原始快照仅保存在 `app/.local/channel-research/`，不进入 Git；未修改任何远端视频或播放列表。

样本含 1,034 个唯一视频：603 公开、350 私密、81 不公开，总时长约 2,367 小时。现有同步返回 1,887 条列表记录，含 853 条重复 ID 记录；这不是用户重复上传的证据。v0.4 同步已按 ID 去重，并拦截分页 token 循环，保留上次完整列表。样本总数尚未与 Studio 总数人工对账。

| 观察 | 下一批应提供的能力 |
| --- | --- |
| 916 个标题含可解析日期，424 个含技术文本 | 标题结构化解析，保存原值，按规则与置信度预览标准标题 |
| 282 个视频有房间号候选、43 个不同候选值 | 与本地直播间对齐，建立稳定来源身份和名称别名台账 |
| 918 个描述为空，标签全部为空 | 按来源模板补全，默认只填空值，保留人工编辑 |
| 110 组同来源/同日期多视频候选 | 场次聚合、画幅与分段对比、本地成品和云端 ID 对账 |
| 19 个视频不超过 10 秒，1 组同标题视频时长相差很大 | 例外筛选与人工对比，不能仅凭标题自动去重 |
| 568 个视频有地区限制字段 | 显示可播放区域，不将其推断为具体版权申诉 |

建议下批按以下顺序落实：

补充复测：新版同步在 2026-09-22 02:11 UTC 返回 1,893 条记录、869 条重复、1,021 个实际可读取的唯一视频；去重后的请求 ID 共 1,024 个，其中 3 个没有视频详情返回。两轮样本数量不同，原因尚未定位，不能把第二轮未出现的记录当作已删除。旧研究快照和升级前数据库备份仍保留；现有界面按最近一次完成的列表展示，旧元信息仍在本地缓存。下一步应先增加跨轮缺失对账及分页稳定性诊断，再实施大规模元信息变更。

1. 只读字段筛选、保存视图、结构化来源/日期候选与本地关联。
2. 播放列表及成员只读同步，研究复用/新建/冲突规则；本次未读取播放列表。
3. “仅补空值”元信息模板和逐条预览，再把批量变更也接入任务中心。
4. 确认来源关系后，连接上传、自动归入列表与补档对账。

Data API 本次数据不包含 Analytics 留存、收入、Content ID 申诉详情或 Studio 内部审核信息；不以这些字段缺失推断账号状态。
