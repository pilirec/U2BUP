[workspace]
members = ["crates/core", "crates/server", "desktop"]
default-members = ["crates/core", "crates/server"]
resolver = "2"

[workspace.package]
version = "0.2.0"
edition = "2021"
license = "GPL-3.0-only"

[profile.release]
opt-level = "s"
lto = "thin"
strip = true
